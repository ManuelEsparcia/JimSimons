"""
simons_core.io — Institutional I/O infrastructure.

    paths.py         Deterministic locator resolution, confinement, normalization
    parquet_store.py Snapshot-based columnar persistence with schema validation
    cache.py         Atomic local cache with SHA-256 keys, byte-LRU, TTL
"""
